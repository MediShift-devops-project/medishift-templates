name: Template - CD Update Manifests

on:
  workflow_call:
    inputs:
      service_name:
        required: true
        type: string
      service_key:
        required: true
        type: string
      image_version:
        required: true
        type: string
      branch:
        required: true
        type: string
    secrets:
      MANIFEST_REPO_PAT:
        required: true
      SMTP_USERNAME:
        required: true
      SMTP_PASSWORD:
        required: true
      ALERT_EMAIL:
        required: true
      DEV_DB_URL:
        required: true
      DEV_JWT_SECRET:
        required: true
      DEV_DB_USER:
        required: true
      DEV_DB_PASSWORD:
        required: true
      DEV_DB_NAME:
        required: true
      PROD_DB_URL:
        required: true
      PROD_JWT_SECRET:
        required: true
      PROD_DB_USER:
        required: true
      PROD_DB_PASSWORD:
        required: true
      PROD_DB_NAME:
        required: true

jobs:
  update-manifests:
    name: Update Helm Values - ${{ inputs.branch == 'main' && 'Production' || 'Development' }}
    runs-on: ubuntu-latest
    steps:
    
      # STEP 1: Set target environment variables
      - name: Set Target Environment
        id: target
        run: |
          if [ "${{ inputs.branch }}" = "main" ]; then
            echo "values_file=medishift-helm/values-prod.yaml" >> $GITHUB_OUTPUT
            echo "environment=production" >> $GITHUB_OUTPUT
            echo "namespace=medishift-prod" >> $GITHUB_OUTPUT
          else
            echo "values_file=medishift-helm/values-dev.yaml" >> $GITHUB_OUTPUT
            echo "environment=development" >> $GITHUB_OUTPUT
            echo "namespace=medishift-dev" >> $GITHUB_OUTPUT
          fi
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Branch:      ${{ inputs.branch }}"
          echo "Service:     ${{ inputs.service_name }}"
          echo "Version:     ${{ inputs.image_version }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # STEP 2: Checkout medishift-manifests repo
      - name: Checkout medishift-manifests Repo
        uses: actions/checkout@v4
        with:
          repository: MediShift-devops-project/medishift-manifests
          token: ${{ secrets.MANIFEST_REPO_PAT }}
          ref: ${{ inputs.branch == 'main' && 'main' || 'develop' }}

      # STEP 3: Install yq
      - name: Install yq
        run: |
          wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          chmod +x /usr/local/bin/yq
          yq --version

      # STEP 4: Update image tag + inject secrets
      - name: Update Helm Values - Tag and Secrets
        run: |
          VALUES_FILE="${{ steps.target.outputs.values_file }}"
          SERVICE_KEY="${{ inputs.service_key }}"
          NEW_TAG="${{ inputs.image_version }}"

          # Select secrets based on branch
          if [ "${{ inputs.branch }}" = "main" ]; then
            DB_URL="${{ secrets.PROD_DB_URL }}"
            JWT_SECRET="${{ secrets.PROD_JWT_SECRET }}"
            DB_USER="${{ secrets.PROD_DB_USER }}"
            DB_PASSWORD="${{ secrets.PROD_DB_PASSWORD }}"
            DB_NAME="${{ secrets.PROD_DB_NAME }}"
          else
            DB_URL="${{ secrets.DEV_DB_URL }}"
            JWT_SECRET="${{ secrets.DEV_JWT_SECRET }}"
            DB_USER="${{ secrets.DEV_DB_USER }}"
            DB_PASSWORD="${{ secrets.DEV_DB_PASSWORD }}"
            DB_NAME="${{ secrets.DEV_DB_NAME }}"
          fi

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "File:    ${VALUES_FILE}"
          echo "Service: ${SERVICE_KEY}"
          echo "Old tag: $(yq ".${SERVICE_KEY}.tag" ${VALUES_FILE})"
          echo "New tag: ${NEW_TAG}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          # Update image tag
          yq -i ".${SERVICE_KEY}.tag = \"${NEW_TAG}\"" ${VALUES_FILE}

          # Inject secrets from GitHub Secrets
          yq -i ".secrets.create = true" ${VALUES_FILE}
          yq -i ".secrets.databaseUrl = \"${DB_URL}\"" ${VALUES_FILE}
          yq -i ".secrets.jwtSecret = \"${JWT_SECRET}\"" ${VALUES_FILE}
          yq -i ".secrets.postgresUser = \"${DB_USER}\"" ${VALUES_FILE}
          yq -i ".secrets.postgresPassword = \"${DB_PASSWORD}\"" ${VALUES_FILE}
          yq -i ".secrets.postgresDb = \"${DB_NAME}\"" ${VALUES_FILE}

          echo "Tag updated:     $(yq ".${SERVICE_KEY}.tag" ${VALUES_FILE})"
          echo "Secrets injected into ${VALUES_FILE}"
          echo "secrets.create:  $(yq ".secrets.create" ${VALUES_FILE})"

      # STEP 5: Commit and push with retry
      - name: Commit and Push With Retry
        run: |
          git config user.name "MediShift DevOps Bot"
          git config user.email "devops-bot@medishift.com"

          git add ${{ steps.target.outputs.values_file }}

          git commit -m "deploy(${{ inputs.service_key }}): update tag to ${{ inputs.image_version }}

          Service:     ${{ inputs.service_name }}
          Environment: ${{ steps.target.outputs.environment }}
          Namespace:   ${{ steps.target.outputs.namespace }}
          Version:     ${{ inputs.image_version }}
          Branch:      ${{ inputs.branch }}
          Run:         ${{ github.run_id }}"

          for i in 1 2 3 4 5; do
            echo "Push attempt ${i}..."
            if git push origin HEAD; then
              echo "Push successful on attempt ${i}"
              break
            fi
            if [ $i -eq 5 ]; then
              echo "All 5 push attempts failed"
              exit 1
            fi
            echo "Retrying in $((i * 3)) seconds..."
            git pull --rebase origin HEAD
            sleep $((i * 3))
          done

      # STEP 6: Verify values file looks correct
      - name: Verify Values File
        run: |
          VALUES_FILE="${{ steps.target.outputs.values_file }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Final tag:            $(yq ".${{ inputs.service_key }}.tag" ${VALUES_FILE})"
          echo "secrets.create:       $(yq ".secrets.create" ${VALUES_FILE})"
          echo "secrets.postgresUser: $(yq ".secrets.postgresUser" ${VALUES_FILE})"
          echo "secrets.postgresDb:   $(yq ".secrets.postgresDb" ${VALUES_FILE})"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Values file verified"
          echo "ArgoCD will auto-sync and apply secrets to cluster"

      # STEP 7: Failure email
      - name: Send Failure Email - CD
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "FAILED - CD Deploy - ${{ inputs.service_name }} - ${{ steps.target.outputs.environment }}"
          to: ${{ secrets.ALERT_EMAIL }}
          from: MediShift DevOps <${{ secrets.SMTP_USERNAME }}>
          body: |
            CD Manifest Update FAILED

            Service:     ${{ inputs.service_name }}
            Environment: ${{ steps.target.outputs.environment }}
            Version:     ${{ inputs.image_version }}
            Branch:      ${{ inputs.branch }}
            Workflow:    ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            Action Required:
            - Check MANIFEST_REPO_PAT token is valid
            - Check medishift-manifests repo permissions
            - Verify yq can update the values file
            - Check GitHub Org Secrets are set correctly

            -- MediShift DevOps Pipeline
